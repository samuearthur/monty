#include "monty.h"
/**
 * _nop - nothing with the stack
 * @stack:Double pointer to the stack
 * @line_number: value for includein the stack
 * Return: Nothing.
 */
void _nop(sstack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
